# ============================================================================
# Experiment config -- AstraFlow service + Trainer
# Experiment: math / qwen3-1.7b-m2po-2gpus-lora
#
# Qwen3-1.7B math RL with M2PO, ctx 7k, lr 5e-6, full TCP weight transfer,
# LoRA (rank 32) on the actor. Mirrors the -full recipe; only the actor LoRA
# block and the SGLang LoRA flags (raas.yaml) differ.
#
# GPU layout (default, 2 GPUs):
#   SERVICE_CUDA_VISIBLE_DEVICES=0 -> RaaS (model0 dp=1)
#   TRAINER_MODEL0_GPUS=1          -> Trainer model0 (FSDP, 1 GPU)
# ============================================================================

# -- Experiment: identity, model, shared settings --
experiment:
  experiment_name: astraflow-math
  trial_name: qwen3-1.7b-m2po-2gpus-lora
  fileroot: ./data-experiments/${experiment.experiment_name}/${experiment.trial_name}

  model_path: "Qwen/Qwen3-1.7B"
  tokenizer_path: "Qwen/Qwen3-1.7B"
  seed: 1
  dtype: bfloat16
  weight_transfer_mode: tcp
  # LoRA must use full transfer (delta is not used with LoRA).
  weight_transfer_strategies: full

# -- RaaS: what to generate (inference-level config) --
# model keys here also determine expected_model_ids for AstraFlow service
raas:
  models:
    model0:
      backend: sglang
      gconfig:
        n_samples: 8
        temperature: 1.0
        max_new_tokens: 4000
        min_new_tokens: 0

# -- AstraFlow: data pipeline --
# auto-derives: expected_model_ids from raas.models keys
# auto-derives: dump_dir from experiment.fileroot
dataflow:
  host: "0.0.0.0"
  port: 8000

  buffer:
    size: 10000
    replay_size: 10000
    replay_ratio: 0
    # LoRA needs near-on-policy rollouts: its alpha/rank scaling makes each
    # weight update ~8x larger than full-FT at the same lr, so the staleness
    # full-FT tolerates pushes LoRA past the stability edge (entropy collapse,
    # destructive adapter). Keep staleness tight; pair with ppo_n_minibatches=1.
    max_staleness: 1
    filter_function: filter_zero_adv

  rollout_dataset:
    dataset_fn: "astraflow.dataflow.dataset.deepscaler:get_deepscaler_rl_dataset"
    max_length: 2000

  workflow_spec:
    workflow_cls: "rlvr"
    reward_fn: "math_verify"
    enable_thinking: false

  eval_workflows:
    math_eval:
      workflow_cls: "rlvr"
      reward_fn: "math_verify"
      enable_thinking: false
      gconfig_overrides:
        temperature: 0.6
        n_samples: 1

  eval_datasets:
    aime24:
      dataset_fn: "astraflow.dataflow.dataset.aime24x4:get_aime_2024x4_test_dataset"
      max_length: 2000
      repeat: 4
      eval_workflow: math_eval
    aime25:
      dataset_fn: "astraflow.dataflow.dataset.aime25x4:get_aime_2025x4_test_dataset"
      max_length: 2000
      repeat: 4
      eval_workflow: math_eval
    amc:
      dataset_fn: "astraflow.dataflow.dataset.amc24:get_amc_2024x4_test_dataset"
      max_length: 2000
      repeat: 4
      eval_workflow: math_eval
    minerva:
      dataset_fn: "astraflow.dataflow.dataset.minervamath:get_minerva_math_test_dataset"
      max_length: 2000
      repeat: 4
      eval_workflow: math_eval
    math500:
      dataset_fn: "astraflow.dataflow.dataset.math500:get_math500_test_dataset"
      max_length: 2000
      repeat: 4
      eval_workflow: math_eval

# -- Trainer base: shared config --
# auto-derives from experiment: experiment_name, trial_name, fileroot,
#   tokenizer_path, seed, dtype, weight_transfer_mode
# auto-derives from raas.models.<model_id>: actor.path, actor.max_new_tokens,
#   ref.path
# auto-derives: saver, recover, stats_logger fields from experiment section
# auto-derives: cluster.name_resolve from experiment.fileroot
# auto-derives: trial_name suffix from model_id (e.g. trial_name-model0)
trainer_base:
  total_train_steps: 800
  train_batch_size: 256
  n_samples: 8
  engine:
    backend: fsdp
    data_parallel_size: 1

  actor:
    gradient_checkpointing: true
    # Recompute old_logp in the trainer so the PPO ratio is computed against
    # the exact policy being optimized (with ppo_n_minibatches=1 this pins the
    # importance weight at 1.0 — fully on-policy, required for stable LoRA).
    recompute_logprob: true
    # -- LoRA --
    # Explicit target_modules (not "all-linear") so the SGLang launch
    # `lora_target_modules` in raas.yaml matches deterministically.
    use_lora: true
    lora_rank: 32
    lora_alpha: 16
    target_modules: [q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj]
    mb_spec:
      max_tokens_per_mb: 17408
    optimizer:
      type: adam
      lr: 5e-6
      weight_decay: 0.01
      beta1: 0.9
      beta2: 0.999
      eps: 1e-8
      lr_scheduler_type: constant
      gradient_clipping: 1.0
    # PPO / M2PO algorithm
    m2_threshold: 0.01
    eps_clip: 100.0
    eps_clip_higher: 100.0
    reward_scaling: 1
    reward_bias: 0
    kl_ctl: 0.00
    kl_penalty_coef: 0.001
    # On-policy single update: avoids within-batch policy drift, which LoRA's
    # amplified effective step does not tolerate (see max_staleness note above).
    ppo_n_minibatches: 1
    reward_norm: { mean_level: group, std_level: group }
    adv_norm: { mean_level: batch, std_level: batch }

  ref:
    mb_spec:
      max_tokens_per_mb: 17408

  recover:
    mode: auto
    freq_steps: 25

  evaluator:
    eval_at_start: false
    freq_steps: 25

  stats_logger:
    wandb:
      mode: online
      id_suffix: "uid"

# -- Trainer for model0 -- only overrides --
trainer_model0:
  model_id: model0
  stats_logger:
    wandb:
      tags: ["m2po", "math", "astraflow-v2", "qwen3-1.7b", "tcp", "ctx7k", "lora", "2gpus", "examples", "sglang-d1", "fsdp-d1"]
