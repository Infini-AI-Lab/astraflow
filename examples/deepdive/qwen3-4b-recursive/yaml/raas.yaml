# ============================================================================
# RaaS config — Inference serving instance (hardware/resources)
# Experiment: deepdive / qwen3-4b-recursive
#
# Hardware: 4x GPU, TP=1; model0 DP=4
# Merged with experiment.yaml at launch (--config experiment.yaml --config raas.yaml)
# ============================================================================

rollout:
  max_concurrent_rollouts: 1024
  max_concurrent_evals: 64
  pause_grace_period: 3
  enable_adaptive_availability: true
  target_waiting_queue_per_dp: 4
  adaptive_step_size: 4
  load_cache_ttl_ms: 100

engine:
  model0:
    backend: sglang
    data_parallel_size: 4

sglang:
  # 40K context — matches platoon's DeepDive recipe (context_length: 40001).
  # DeepDive rollouts can be long: many search calls + observations + the
  # agent's own reasoning. 32K was insufficient in platoon's experience.
  context_length: 40001
  mem_fraction_static: 0.8         # platoon's default
  max_running_requests: null
  skip_tokenizer_init: true
